part of 'constants.dart';

class Durations {
  static const Duration toastTimeInSec = Duration(seconds: 4);
  static const Duration otpTimeoutInSec = Duration(seconds: 30);
  static const Duration otpAnimationInMS = Duration(milliseconds: 300);
  static const Duration switcherAnimationInMS = Duration(milliseconds: 500);
  static const Duration pageForwardTransitionInMS = Duration(milliseconds: 500);
  static const Duration pageReverseTransitionInMS = Duration(milliseconds: 500);
}
